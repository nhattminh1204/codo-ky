import 'package:flutter/material.dart';

class ItinerarySetupScreen extends StatelessWidget {
  const ItinerarySetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Thiết lập lịch trình'),
      ),
      body: const Center(
        child: Text('TODO: Thiết lập lịch trình (Itinerary Setup)'),
      ),
    );
  }
}
